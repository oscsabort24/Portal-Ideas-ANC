# Copiar como .env.docker (gitignorado) y ajustar la contraseña.
# Este archivo alimenta SOLO al contenedor de docker-compose.yml — es
# independiente del .env que usa la app (core/config.py), aunque la
# contraseña debe coincidir entre ambos para que la app pueda conectarse.
SA_PASSWORD=CambiaEstaClave123!
