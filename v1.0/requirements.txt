fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pyodbc==5.2.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
email-validator==2.3.0

# Validación de tokens de Microsoft Entra ID — agregado como andamiaje.
# La implementación real (core/auth.py) está pendiente de credenciales de Azure AD.
python-jose[cryptography]==3.3.0

# Hashing del PIN personal de admin (criterios/seguridad.py)
bcrypt==4.2.1

# python-multipart: requerido por FastAPI para leer UploadFile/Form (criterios/router.py)
python-multipart==0.0.20

# python-docx: generación de los 6 documentos formales (documentos/generadores.py)
python-docx==1.1.2

# graphviz: diagrama BPMN visual (documentos/generadores.py). Requiere el
# ejecutable "dot" de Graphviz instalado en el sistema, no solo este paquete
# — ver https://graphviz.org/download/ (Windows) o el gestor de paquetes de
# tu distro en Linux/macOS.
graphviz==0.21

# anthropic: SDK oficial de Claude (core/claude_client.py), usado cuando
# CLAUDE_STUB_MODE=false.
anthropic==0.116.0

# jinja2: plantillas HTML de los 6 documentos formales (documentos/plantillas_html.py),
# usadas para /preview, generación de PDF (Playwright) y como referencia visual
# para los generadores .docx.
jinja2==3.1.6

# playwright: renderiza las plantillas HTML a PDF con Chromium headless
# (documentos/pdf.py). Requiere además "playwright install chromium" una
# vez por entorno — no alcanza con el paquete Python solo (mismo tipo de
# paso adicional que "dot" de Graphviz, pero sin permisos de administrador).
playwright==1.61.0
